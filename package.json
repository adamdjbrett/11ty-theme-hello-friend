{
  "name": "11ty-theme-hello-friend",
  "version": "4.0.0",
  "description": "Hello Friend theme ported to Eleventy 3.1.2 with ESM, Nunjucks, Luxon, and Pagefind.",
  "type": "module",
  "author": "Radosław Kozieł <@panr> <radoslaw.koziel@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "clean": "rm -rf _site .11ty-cache .11ty-search",
    "build:css": "sass --no-source-map --style=compressed assets/css/style.scss static/style.css",
    "build:site": "eleventy",
    "build:search": "pagefind --site _site --glob \"**/*.html\"",
    "build": "npm run clean && npm run build:css && npm run build:site && npm run build:search",
    "dev": "npm run build:css && eleventy --serve",
    "test": "npm run build"
  },
  "dependencies": {
    "@11ty/eleventy": "3.1.2",
    "@11ty/eleventy-plugin-rss": "^2.0.2",
    "luxon": "^3.7.2",
    "pagefind": "^1.3.0",
    "sass": "^1.93.2"
  }
}
